<ul class="navigation">
	<% if params[:action] == "index"%>
	 <li class="selected">末完了タスク</li>
	 <% else %>
	 <li><%= link_to "末完了タスク", [@category, :tasks] %></li>
	<% end %>
	<% if params[:action] == "done" %>
	<li class="selected">完了タスク</li>
	<% else %>
	<li><%= link_to "完了タスク", [:done, @category, :tasks] %></li>
	<% end %>
</ul>
